<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StreamCode</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet">
    <link rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.0.7/css/all.css">
  </head>

  <body class="bg-dark text-light">
    <header class="py-4 bg-primary text-center">
      <h1 class="display-4 fw-bold">
        <i class="fas fa-film"></i> StreamCode
      </h1>
      <p class="lead">Discover the Best Movies for Programmers & Tech
        Enthusiasts</p>
    </header>

    <div class="container my-5">
      <div class="row justify-content-center">
        <div class="col-md-6">
          <h3 class="mb-4 text-center">Edit Your Movie</h3>

          <form action="/update" method="post" enctype="multipart/form-data"
            class="border p-4 rounded bg-secondary text-light">
            <input type="hidden" name="editid" value="<%= singal?.id || '' %>">
            <div class="mb-3">
              <label for="movie-title" class="form-label">Movie Name</label>
              <input type="text" id="movie-title" name="name"
                class="form-control" placeholder="Enter movie name"
                value="<%= singal?.name || '' %>" required>
            </div>
            <div class="mb-3">
              <label for="movie-desc" class="form-label">Movie
                Description</label>
              <input type="text" id="movie-desc" name="desc"
                class="form-control" placeholder="Enter movie description"
                value="<%= singal?.desc || '' %>" required>
            </div>
            <div class="mb-3">
              <label for="movie-price" class="form-label">Price</label>
              <input type="text" id="movie-price" name="price"
                class="form-control" placeholder="Enter price"
                value="<%= singal?.price || '' %>" required>
            </div>
            <div class="mb-3">
              <label for="movie-img" class="form-label">Upload Image</label>
              <input type="file" id="movie-img" name="image"
                class="form-control">
            </div>
            <div class="mb-3 text-center">
              <img src="<%= singal?.image || '/default-image-path.jpg' %>"
                class="img-thumbnail" style="max-width: 150px;"
                alt="Movie Image">
            </div>
            <button type="submit" class="btn btn-primary w-100">
              Submit <i class="fas fa-check"></i>
            </button>
          </form>
        </div>
      </div>
    </div>

    <footer class="py-4 bg-primary text-center">
      <p class="mb-0">© 2024 StreamCode. All Rights Reserved.</p>
    </footer>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>

</html>
